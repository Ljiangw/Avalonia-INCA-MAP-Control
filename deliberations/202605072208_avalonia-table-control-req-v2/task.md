# 任务：迭代 Avalonia 标定数据表格编辑控件需求文档

## 背景

当前有一份需求文档 `C:\Users\jiangwei\Documents\C#\INCA_MAP_Control\requirements\202605072136_avalonia-table-control\req_v1.md`，描述了基于 Avalonia UI 框架的标定数据表格编辑控件需求。

同时有一份 todo 文件 `C:\Users\jiangwei\Documents\C#\INCA_MAP_Control\requirements\202605072136_avalonia-table-control\todo.md`，记录了用户最新的迭代需求。

## 迭代要求

根据 todo.md 中的内容，对 req_v1.md 进行迭代更新，生成 req_v2.md。todo 中的核心变更点如下：

1. **控件拆分**：应制作 3 个独立控件，分别支持 1 维（曲线）、2 维（MAP）和 3 维数据展示与编辑。

2. **顶部信息栏与变量选择**：
   - 每个控件的顶部信息栏左侧为变量选择下拉框
   - 3 个控件都可以接收 `List` 形式的数据
   - List 中的每个数据项都成为下拉框中的选项
   - 通过选中下拉框的选项，来展示对应的数据

3. **3 维表格展示方式（关键变更）**：
   - 3 维表格在 3D 曲面图和数据表格之间，以列的形式插入 Z 轴数据
   - 通过点击 Z 轴数据，显示在该 Z 轴数据条件下的 (x, y) → value 数据
   - 本质是多张 2 维 MAP，通过选择 Z 轴数据来展示不同的 2 维 MAP
   - **注意**：这与 req_v1.md 中当前描述的 "嵌套行表格" 方式不同，需要按 todo 中的新描述重新设计

## 产出要求

生成一份完整、自洽的需求文档 `req_v2.md`，需满足：

1. **完整性**：包含所有必要的章节（概述、各维度控件详细需求、公共交互、技术约束、范围边界等）
2. **一致性**：3 个控件在顶部信息栏、单元格编辑、选中高亮、数据绑定等公共功能上保持一致
3. **清晰性**：3 维控件的展示方式需准确反映 todo 中的描述（Z 轴以列形式插入，点击切换不同的 2 维 MAP）
4. **可追溯性**：对从 v1 到 v2 的变更点应有所体现

产出文件路径：由执行 agent 自行决定输出路径（写入当前 deliberation 工作目录即可）。
