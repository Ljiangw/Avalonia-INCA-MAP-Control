# 任务描述

## 目标

基于现有需求文档 `C:\Users\jiangwei\Documents\C#\INCA_MAP_Control\requirements\202605072136_avalonia-table-control\req_v2.md`，根据修改要求迭代生成一份新的需求文档（req_v3）。

## 参考源文件

- **当前需求文档**：`C:\Users\jiangwei\Documents\C#\INCA_MAP_Control\requirements\202605072136_avalonia-table-control\req_v2.md`
  - 文档标题：Avalonia 标定数据表格编辑控件需求 v2
  - 包含内容：3 个独立控件（CurveEditor、MapEditor、CubeEditor）的详细需求规格

## 修改要求（来自 todo.md）

针对 **3 维控件（CubeEditor）** 的布局设计，进行以下修改：

1. **取消 Z 轴数据列作为独立中间区域的设计**
   - v2 中 CubeEditor 主内容区从左到右分为：3D 曲面预览图 → Z 轴数据列 → 2 维数据表格
   - **修改为**：Z 轴数据不应以独立的列/列表形式插入在 3D 曲面图和数据表格之间

2. **Z 轴数据整合到数据表格中**
   - 在数据表格中，**y 轴那一列的左边**，再添加一个 **z 轴的列**
   - 从视觉上看，z 轴的列应该与 y 轴和 x 轴大小样式一致
   - 这意味着 3 维 CubeEditor 的数据表格将同时包含 z 轴、y 轴和 x 轴的信息

3. **需要进一步明确和细化的交互设计**
   - 如何在表格中展示多层 z 切片数据？是每张 z 切片展开显示，还是通过某种方式聚合展示？
   - z 轴列的数值如何与右侧的 x-z 数据行对应？
   - 3D 曲面图与表格的联动关系是否需要调整？
   - 选中高亮逻辑在新增 z 轴列后如何运作？

## 产出要求

生成一份完整的需求文档 `req_v3.md`，应满足：

1. **继承 v2 的全部内容**，保留 v2 中除 CubeEditor 布局外的所有设计（CurveEditor、MapEditor、公共架构、技术约束、范围边界等保持不变）
2. **重新设计 CubeEditor 的「布局」和「数据表格」章节**，准确反映 todo 的修改意图
3. **新增或更新「版本变更说明」**，记录 v2 → v3 的变更点
4. **更新「推断与待确认事项」**，将因本次修改产生的新设计决策点列出
5. **文档结构与 v2 保持一致**，仅在必要处调整
6. 产出的文档应完整、自洽、无歧义，可直接作为开发依据
